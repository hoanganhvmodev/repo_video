import { FC } from 'react'
import Cms from '@components/Cms/Cms'

// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface CmsProps {}

// eslint-disable-next-line no-empty-pattern
const admin: FC<CmsProps> = ({}) => {
  return <Cms />
}

export default admin
